!
! auto-generated by ops_fortran.py
!
MODULE MBLOCK_POPULATE_KERNEL_MODULE
USE OPS_FORTRAN_DECLARATIONS
USE OPS_FORTRAN_RT_SUPPORT

USE OPS_CONSTANTS
USE ISO_C_BINDING

INTEGER(KIND=4) xdim1
#define OPS_ACC1(x,y) (x+xdim1_mblock_populate_kernel*(y)+1)


contains

!user function
!DEC$ ATTRIBUTES FORCEINLINE :: mblock_populate_kernel
subroutine mblock_populate_kernel(val, idx)
  IMPLICIT NONE
  real (kind=8), DIMENSION(1):: val
  INTEGER(kind=4), DIMENSION(1), INTENT(IN) :: idx

  val(OPS_ACC1(0,0)) = idx(1)+20*idx(2)
end subroutine

#undef OPS_ACC1



subroutine mblock_populate_kernel_wrap( &
& opsDat1Local, &
& idx, &
& dat1_base, &
& start, &
& end )
  IMPLICIT NONE
  real(8)opsDat1Local(*)
  integer(4) idx(2),idx_local(2)
  integer dat1_base
  integer(4) start(2)
  integer(4) end(2)
  integer n_x, n_y

  DO n_y = 1, end(2)-start(2)+1
    idx_local(2) = idx(2) + n_y - 1
    DO n_x = 1, end(1)-start(1)+1
      idx_local(1) = idx(1) + n_x - 1
      call mblock_populate_kernel( &
      & opsDat1Local(dat1_base+(n_x-1)*1 + (n_y-1)*xdim1*1), &
      & idx_local )
    END DO
  END DO
end subroutine

!host subroutine
subroutine mblock_populate_kernel_host( userSubroutine, block, dim, range, &
& opsArg1, &
& opsArg2)
  IMPLICIT NONE
  character(kind=c_char,len=*), INTENT(IN) :: userSubroutine
  type ( ops_block ), INTENT(IN) :: block
  integer(kind=4), INTENT(IN):: dim
  integer(kind=4)   , DIMENSION(dim), INTENT(IN) :: range

  type ( ops_arg )  , INTENT(IN) :: opsArg1
  real(8), POINTER, DIMENSION(:) :: opsDat1Local
  integer(kind=4) :: opsDat1Cardinality
  integer(kind=4) , POINTER, DIMENSION(:)  :: dat1_size
  integer(kind=4) :: dat1_base
  integer ydim1

  type ( ops_arg )  , INTENT(IN) :: opsArg2

  integer n_x, n_y
  integer start(2)
  integer end(2)
  integer idx(2)
  integer(kind=4) :: n

  type ( ops_arg ) , DIMENSION(2) :: opsArgArray

  opsArgArray(1) = opsArg1
  opsArgArray(2) = opsArg2

#ifdef OPS_MPI
  call getRange(block, start, end, range)
#else
  DO n = 1, 2
    start(n) = range(2*n-1)
    end(n) = range(2*n);
  END DO
#endif

#ifdef OPS_MPI
  call getIdx(block,start,idx)
#else
  idx(1) = start(1)
  idx(2) = start(2)
#endif

  call c_f_pointer(getDatSizeFromOpsArg(opsArg1),dat1_size,(/dim/))
  xdim1 = dat1_size(1)
  ydim1 = dat1_size(2)
  opsDat1Cardinality = opsArg1%dim * xdim1 * ydim1
  dat1_base = getDatBaseFromOpsArg2D(opsArg1,start,1)
  call c_f_pointer(opsArg1%data,opsDat1Local,(/opsDat1Cardinality/))

  call ops_H_D_exchanges_host(opsArgArray,2)
  call ops_halo_exchanges(opsArgArray,2,range)
  call ops_H_D_exchanges_host(opsArgArray,2)

  call mblock_populate_kernel_wrap( &
  & opsDat1Local, &
  & idx, &
  & dat1_base, &
  & start, &
  & end )

  call ops_set_dirtybit_host(opsArgArray, 2)
  call ops_set_halo_dirtybit3(opsArg1,range)

end subroutine
END MODULE
