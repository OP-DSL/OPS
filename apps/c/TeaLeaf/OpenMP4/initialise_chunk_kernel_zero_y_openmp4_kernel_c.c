//
// auto-generated by ops.py
//
#include "./OpenMP4/tea_leaf_common.h"
#include <omp.h>
#define OPS_GPU

extern int xdim0_initialise_chunk_kernel_zero_y;

#undef OPS_ACC0

#define OPS_ACC0(x, y)                                                         \
  (n_x * 0 * 1 + n_y * xdim0_initialise_chunk_kernel_zero_y * 1 * 1 + x +      \
   xdim0_initialise_chunk_kernel_zero_y * (y))

// user function

void initialise_chunk_kernel_zero_y_c_wrapper(double *p_a0, int x_size,
                                              int y_size) {
#ifdef OPS_GPU

#pragma omp target teams distribute parallel for
#endif
  for (int i = 0; i < y_size * x_size; i++) {
    const int id =
        omp_get_num_threads() * omp_get_team_num() + omp_get_thread_num();
    const int n_x = id % x_size;
    const int n_y = id / x_size;
    double *var = p_a0;

    *var = 0.0;
  }
}
#undef OPS_ACC0
