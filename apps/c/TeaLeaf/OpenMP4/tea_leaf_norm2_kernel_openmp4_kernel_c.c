//
// auto-generated by ops.py
//
#include "./OpenMP4/tea_leaf_common.h"
#include <omp.h>
#define OPS_GPU

extern int xdim0_tea_leaf_norm2_kernel;

#undef OPS_ACC0

#define OPS_ACC0(x, y)                                                         \
  (n_x * 1 * 1 + n_y * xdim0_tea_leaf_norm2_kernel * 1 * 1 + x +               \
   xdim0_tea_leaf_norm2_kernel * (y))

// user function

void tea_leaf_norm2_kernel_c_wrapper(double *p_a0, double *p_a1, int x_size,
                                     int y_size) {
  double p_a1_0 = p_a1[0];
#ifdef OPS_GPU

#pragma omp target teams distribute parallel for map(tofrom : p_a1_0)          \
    reduction(+ : p_a1_0)
#endif
  for (int i = 0; i < y_size * x_size; i++) {
    const int id =
        omp_get_num_threads() * omp_get_team_num() + omp_get_thread_num();
    const int n_x = id % x_size;
    const int n_y = id / x_size;
    const double *x = p_a0;

    double *norm = &p_a1_0;

    *norm = *norm + x[OPS_ACC0(0, 0)] * x[OPS_ACC0(0, 0)];
  }
  p_a1[0] = p_a1_0;
}
#undef OPS_ACC0
