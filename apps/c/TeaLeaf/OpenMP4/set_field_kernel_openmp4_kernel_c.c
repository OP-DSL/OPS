//
// auto-generated by ops.py
//
#include "./OpenMP4/tea_leaf_common.h"
#include <omp.h>
#define OPS_GPU

extern int xdim0_set_field_kernel;
extern int xdim1_set_field_kernel;

#undef OPS_ACC0
#undef OPS_ACC1

#define OPS_ACC0(x, y)                                                         \
  (n_x * 1 * 1 + n_y * xdim0_set_field_kernel * 1 * 1 + x +                    \
   xdim0_set_field_kernel * (y))
#define OPS_ACC1(x, y)                                                         \
  (n_x * 1 * 1 + n_y * xdim1_set_field_kernel * 1 * 1 + x +                    \
   xdim1_set_field_kernel * (y))

// user function

void set_field_kernel_c_wrapper(double *p_a0, double *p_a1, int x_size,
                                int y_size) {
#ifdef OPS_GPU

#pragma omp target teams distribute parallel for
#endif
  for (int i = 0; i < y_size * x_size; i++) {
    const int id =
        omp_get_num_threads() * omp_get_team_num() + omp_get_thread_num();
    const int n_x = id % x_size;
    const int n_y = id / x_size;
    const double *energy0 = p_a0;

    double *energy1 = p_a1;

    energy1[OPS_ACC1(0, 0)] = energy0[OPS_ACC0(0, 0)];
  }
}
#undef OPS_ACC0
#undef OPS_ACC1
