//
// auto-generated by ops.py
//
#include "./OpenMP4/tea_leaf_common.h"

#define OPS_GPU

extern int xdim0_initialise_chunk_kernel_zero;

#undef OPS_ACC0

#define OPS_ACC0(x, y) (x + xdim0_initialise_chunk_kernel_zero * (y))

// user function

void initialise_chunk_kernel_zero_c_wrapper(double *p_a0, int base0, int tot0,
                                            int x_size, int y_size) {
#ifdef OPS_GPU

#pragma omp target teams distribute parallel for num_teams(OPS_threads)        \
    thread_limit(OPS_threads_for_block) schedule(static, 1)
#endif
  for (int i = 0; i < y_size * x_size; i++) {
    int n_x = i % x_size;
    int n_y = i / x_size;
    double *var = p_a0 + base0 + n_x * 1 * 1 +
                  n_y * xdim0_initialise_chunk_kernel_zero * 1 * 1;

    *var = 0.0;
  }
}
#undef OPS_ACC0
