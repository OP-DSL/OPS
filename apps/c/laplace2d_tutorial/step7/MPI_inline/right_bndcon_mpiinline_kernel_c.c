//
// auto-generated by ops.py
//
#include "./MPI_inline/laplace2d_common.h"

int xdim0_right_bndcon;

#define OPS_ACC0(x, y) (n_x * 1 + x + (n_y * 1 + (y)) * xdim0_right_bndcon)
// user function

void right_bndcon_c_wrapper(double *restrict A, int *restrict idx, int arg_idx0,
                            int arg_idx1, int x_size, int y_size) {
#pragma omp parallel for
  for (int n_y = 0; n_y < y_size; n_y++) {
    for (int n_x = 0; n_x < x_size; n_x++) {
      int idx[] = {arg_idx0 + n_x, arg_idx1 + n_y};

      A[OPS_ACC0(0, 0)] = sin(pi * (idx[1] + 1) / (jmax + 1)) * exp(-pi);
    }
  }
}
#undef OPS_ACC0
