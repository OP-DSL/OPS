//
// auto-generated by ops.py
//

#define OPS_GPU

int xdim0_right_bndcon;

//user function
inline 
void right_bndcon(ptr_double A, const int *idx) {
  OPS_ACC(A, 0,0) = sin(pi * (idx[1]+1) / (jmax+1))*exp(-pi);
}


void right_bndcon_c_wrapper(
  double *p_a0,
  int *p_a1,
  int arg_idx0, int arg_idx1,
  int x_size, int y_size) {
  #ifdef OPS_GPU
  #pragma acc parallel deviceptr(p_a0)
  #pragma acc loop
  #endif
  for ( int n_y=0; n_y<y_size; n_y++ ){
    #ifdef OPS_GPU
    #pragma acc loop
    #endif
    for ( int n_x=0; n_x<x_size; n_x++ ){
      int arg_idx[] = {arg_idx0+n_x, arg_idx1+n_y};
      ptr_double ptr0 = {  p_a0 + n_x*1*1 + n_y*xdim0_right_bndcon*1*1, xdim0_right_bndcon};
      right_bndcon( ptr0,
          arg_idx );

    }
  }
}
