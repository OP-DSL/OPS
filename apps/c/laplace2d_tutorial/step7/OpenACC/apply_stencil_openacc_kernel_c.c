//
// auto-generated by ops.py
//
#include "./OpenACC/laplace2d_common.h"

#define OPS_GPU

int xdim0_apply_stencil;
int xdim1_apply_stencil;

#undef OPS_ACC0
#undef OPS_ACC1

#define OPS_ACC0(x, y) (x + xdim0_apply_stencil * (y))
#define OPS_ACC1(x, y) (x + xdim1_apply_stencil * (y))

// user function
inline void apply_stencil(const double *A, double *Anew, double *error) {
  Anew[OPS_ACC1(0, 0)] = 0.25f * (A[OPS_ACC0(1, 0)] + A[OPS_ACC0(-1, 0)] +
                                  A[OPS_ACC0(0, -1)] + A[OPS_ACC0(0, 1)]);
  *error = fmax(*error, fabs(Anew[OPS_ACC1(0, 0)] - A[OPS_ACC0(0, 0)]));
}

#undef OPS_ACC0
#undef OPS_ACC1

void apply_stencil_c_wrapper(double *p_a0, double *p_a1, double *p_a2,
                             int x_size, int y_size) {
  double p_a2_0 = p_a2[0];
#ifdef OPS_GPU
#pragma acc parallel deviceptr(p_a0, p_a1) reduction(max : p_a2_0)
#pragma acc loop reduction(max : p_a2_0)
#endif
  for (int n_y = 0; n_y < y_size; n_y++) {
#ifdef OPS_GPU
#pragma acc loop reduction(max : p_a2_0)
#endif
    for (int n_x = 0; n_x < x_size; n_x++) {
      apply_stencil(p_a0 + n_x * 1 * 1 + n_y * xdim0_apply_stencil * 1 * 1,
                    p_a1 + n_x * 1 * 1 + n_y * xdim1_apply_stencil * 1 * 1,
                    &p_a2_0);
    }
  }
  p_a2[0] = p_a2_0;
}
