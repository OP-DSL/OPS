//
// auto-generated by ops.py
//
#include "./MPI_inline/clover_leaf_common.h"

int xdim0_flux_calc_kernely;
int xdim1_flux_calc_kernely;
int xdim2_flux_calc_kernely;
int xdim3_flux_calc_kernely;

#define OPS_ACC0(x, y)                                                         \
  (n_x * 1 + n_y * xdim0_flux_calc_kernely * 1 + x +                           \
   xdim0_flux_calc_kernely * (y))
#define OPS_ACC1(x, y)                                                         \
  (n_x * 1 + n_y * xdim1_flux_calc_kernely * 1 + x +                           \
   xdim1_flux_calc_kernely * (y))
#define OPS_ACC2(x, y)                                                         \
  (n_x * 1 + n_y * xdim2_flux_calc_kernely * 1 + x +                           \
   xdim2_flux_calc_kernely * (y))
#define OPS_ACC3(x, y)                                                         \
  (n_x * 1 + n_y * xdim3_flux_calc_kernely * 1 + x +                           \
   xdim3_flux_calc_kernely * (y))

// user function

void flux_calc_kernely_c_wrapper(double *restrict vol_flux_y,
                                 const double *restrict yarea,
                                 const double *restrict yvel0,
                                 const double *restrict yvel1, int x_size,
                                 int y_size) {
#pragma omp parallel for
  for (int n_y = 0; n_y < y_size; n_y++) {
    for (int n_x = 0; n_x < x_size; n_x++) {

      vol_flux_y[OPS_ACC0(0, 0)] =
          0.25 * dt * (yarea[OPS_ACC1(0, 0)]) *
          ((yvel0[OPS_ACC2(0, 0)]) + (yvel0[OPS_ACC2(1, 0)]) +
           (yvel1[OPS_ACC3(0, 0)]) + (yvel1[OPS_ACC3(1, 0)]));
    }
  }
}
#undef OPS_ACC0
#undef OPS_ACC1
#undef OPS_ACC2
#undef OPS_ACC3
