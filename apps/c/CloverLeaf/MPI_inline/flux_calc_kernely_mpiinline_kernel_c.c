//
// auto-generated by ops.py
//

int xdim0_flux_calc_kernely;
int xdim1_flux_calc_kernely;
int xdim2_flux_calc_kernely;
int xdim3_flux_calc_kernely;


#define OPS_ACC0(x,y) (n_x*1 + x + (n_y*1+(y))*xdim0_flux_calc_kernely)
#define OPS_ACC1(x,y) (n_x*1 + x + (n_y*1+(y))*xdim1_flux_calc_kernely)
#define OPS_ACC2(x,y) (n_x*1 + x + (n_y*1+(y))*xdim2_flux_calc_kernely)
#define OPS_ACC3(x,y) (n_x*1 + x + (n_y*1+(y))*xdim3_flux_calc_kernely)
//user function



void flux_calc_kernely_c_wrapper(
  double * restrict vol_flux_y,
  const double * restrict yarea,
  const double * restrict yvel0,
  const double * restrict yvel1,
  int x_size, int y_size) {
  #pragma omp parallel for
  for ( int n_y=0; n_y<y_size; n_y++ ){
    for ( int n_x=0; n_x<x_size; n_x++ ){
      

  vol_flux_y(0,0) = 0.25 * dt * (yarea(0,0)) *
  ( (yvel0(0,0)) + (yvel0(1,0)) + (yvel1(0,0)) + (yvel1(1,0)) );


    }
  }
}
#undef OPS_ACC0
#undef OPS_ACC1
#undef OPS_ACC2
#undef OPS_ACC3

