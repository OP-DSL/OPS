//
// auto-generated by ops.py
//
#include "./OpenMP4/clover_leaf_common.h"
#include <omp.h>
#define OPS_GPU

extern int xdim0_flux_calc_kernely;
extern int xdim1_flux_calc_kernely;
extern int xdim2_flux_calc_kernely;
extern int xdim3_flux_calc_kernely;

#undef OPS_ACC0
#undef OPS_ACC1
#undef OPS_ACC2
#undef OPS_ACC3

#define OPS_ACC0(x, y)                                                         \
  (n_x * 1 * 1 + n_y * xdim0_flux_calc_kernely * 1 * 1 + x +                   \
   xdim0_flux_calc_kernely * (y))
#define OPS_ACC1(x, y)                                                         \
  (n_x * 1 * 1 + n_y * xdim1_flux_calc_kernely * 1 * 1 + x +                   \
   xdim1_flux_calc_kernely * (y))
#define OPS_ACC2(x, y)                                                         \
  (n_x * 1 * 1 + n_y * xdim2_flux_calc_kernely * 1 * 1 + x +                   \
   xdim2_flux_calc_kernely * (y))
#define OPS_ACC3(x, y)                                                         \
  (n_x * 1 * 1 + n_y * xdim3_flux_calc_kernely * 1 * 1 + x +                   \
   xdim3_flux_calc_kernely * (y))

// user function

void flux_calc_kernely_c_wrapper(double *p_a0, double *p_a1, double *p_a2,
                                 double *p_a3, int x_size, int y_size) {
#ifdef OPS_GPU

#pragma omp target teams distribute parallel for schedule(static, 1)
#endif
  for (int i = 0; i < y_size * x_size; i++) {
    int n_x = i % x_size;
    int n_y = i / x_size;
    double *vol_flux_y = p_a0;

    const double *yarea = p_a1;
    const double *yvel0 = p_a2;

    const double *yvel1 = p_a3;

    vol_flux_y[OPS_ACC0(0, 0)] =
        0.25 * dt * (yarea[OPS_ACC1(0, 0)]) *
        ((yvel0[OPS_ACC2(0, 0)]) + (yvel0[OPS_ACC2(1, 0)]) +
         (yvel1[OPS_ACC3(0, 0)]) + (yvel1[OPS_ACC3(1, 0)]));
  }
}
#undef OPS_ACC0
#undef OPS_ACC1
#undef OPS_ACC2
#undef OPS_ACC3
