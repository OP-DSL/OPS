//
// auto-generated by ops.py
//
#include "./OpenMP4/clover_leaf_common.h"

#define OPS_GPU

extern int xdim0_reset_field_kernel1;
extern int xdim1_reset_field_kernel1;
extern int xdim2_reset_field_kernel1;
extern int xdim3_reset_field_kernel1;

#undef OPS_ACC0
#undef OPS_ACC1
#undef OPS_ACC2
#undef OPS_ACC3

#define OPS_ACC0(x, y) (x + xdim0_reset_field_kernel1 * (y))
#define OPS_ACC1(x, y) (x + xdim1_reset_field_kernel1 * (y))
#define OPS_ACC2(x, y) (x + xdim2_reset_field_kernel1 * (y))
#define OPS_ACC3(x, y) (x + xdim3_reset_field_kernel1 * (y))

// user function

void reset_field_kernel1_c_wrapper(double *p_a0, int base0, int tot0,
                                   double *p_a1, int base1, int tot1,
                                   double *p_a2, int base2, int tot2,
                                   double *p_a3, int base3, int tot3,
                                   int x_size, int y_size) {
  int num_blocks = round(((double)x_size * (double)y_size) / 128);
#pragma omp target enter data map(                                             \
    to : p_a0[0 : tot0], p_a1[0 : tot1], p_a2[0 : tot2], p_a3[0 : tot3])
#ifdef OPS_GPU

#pragma omp target map(                                                        \
    to : p_a0[0 : tot0], p_a1[0 : tot1], p_a2[0 : tot2], p_a3[0 : tot3])
#pragma omp teams num_teams(num_blocks) thread_limit(128)
#pragma omp distribute parallel for simd collapse(2) schedule(static, 1)
#endif
  for (int n_y = 0; n_y < y_size; n_y++) {
#ifdef OPS_GPU
#endif
    for (int n_x = 0; n_x < x_size; n_x++) {
      double *density0 =
          p_a0 + base0 + n_x * 1 * 1 + n_y * xdim0_reset_field_kernel1 * 1 * 1;

      const double *density1 =
          p_a1 + base1 + n_x * 1 * 1 + n_y * xdim1_reset_field_kernel1 * 1 * 1;
      double *energy0 =
          p_a2 + base2 + n_x * 1 * 1 + n_y * xdim2_reset_field_kernel1 * 1 * 1;

      const double *energy1 =
          p_a3 + base3 + n_x * 1 * 1 + n_y * xdim3_reset_field_kernel1 * 1 * 1;

      density0[OPS_ACC0(0, 0)] = density1[OPS_ACC1(0, 0)];
      energy0[OPS_ACC2(0, 0)] = energy1[OPS_ACC3(0, 0)];
    }
  }
}
#undef OPS_ACC0
#undef OPS_ACC1
#undef OPS_ACC2
#undef OPS_ACC3
