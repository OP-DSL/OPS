//
// auto-generated by ops.py
//

#define OPS_GPU

int xdim0_revert_kernel;
int xdim1_revert_kernel;
int xdim2_revert_kernel;
int xdim3_revert_kernel;

// user function
#pragma acc routine
inline void revert_kernel(const ptr_double density0, ptr_double density1,
                          const ptr_double energy0, ptr_double energy1) {

  OPS_ACC(density1, 0, 0) = OPS_ACC(density0, 0, 0);
  OPS_ACC(energy1, 0, 0) = OPS_ACC(energy0, 0, 0);
}

void revert_kernel_c_wrapper(double *p_a0, double *p_a1, double *p_a2,
                             double *p_a3, int x_size, int y_size) {
#ifdef OPS_GPU
#pragma acc parallel deviceptr(p_a0, p_a1, p_a2, p_a3)
#pragma acc loop
#endif
  for (int n_y = 0; n_y < y_size; n_y++) {
#ifdef OPS_GPU
#pragma acc loop
#endif
    for (int n_x = 0; n_x < x_size; n_x++) {
      const ptr_double ptr0 = {p_a0 + n_x * 1 * 1 +
                                   n_y * xdim0_revert_kernel * 1 * 1,
                               xdim0_revert_kernel};
      ptr_double ptr1 = {p_a1 + n_x * 1 * 1 + n_y * xdim1_revert_kernel * 1 * 1,
                         xdim1_revert_kernel};
      const ptr_double ptr2 = {p_a2 + n_x * 1 * 1 +
                                   n_y * xdim2_revert_kernel * 1 * 1,
                               xdim2_revert_kernel};
      ptr_double ptr3 = {p_a3 + n_x * 1 * 1 + n_y * xdim3_revert_kernel * 1 * 1,
                         xdim3_revert_kernel};
      revert_kernel(ptr0, ptr1, ptr2, ptr3);
    }
  }
}
