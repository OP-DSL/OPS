//
// auto-generated by ops.py
//
#include "./MPI_inline/clover_leaf_common.h"

int xdim0_ideal_gas_kernel;
int ydim0_ideal_gas_kernel;
int xdim1_ideal_gas_kernel;
int ydim1_ideal_gas_kernel;
int xdim2_ideal_gas_kernel;
int ydim2_ideal_gas_kernel;
int xdim3_ideal_gas_kernel;
int ydim3_ideal_gas_kernel;

#define OPS_ACC0(x, y, z)                                                      \
  (n_x * 1 + x + (n_y * 1 + (y)) * xdim0_ideal_gas_kernel +                    \
   (n_z * 1 + (z)) * xdim0_ideal_gas_kernel * ydim0_ideal_gas_kernel)
#define OPS_ACC1(x, y, z)                                                      \
  (n_x * 1 + x + (n_y * 1 + (y)) * xdim1_ideal_gas_kernel +                    \
   (n_z * 1 + (z)) * xdim1_ideal_gas_kernel * ydim1_ideal_gas_kernel)
#define OPS_ACC2(x, y, z)                                                      \
  (n_x * 1 + x + (n_y * 1 + (y)) * xdim2_ideal_gas_kernel +                    \
   (n_z * 1 + (z)) * xdim2_ideal_gas_kernel * ydim2_ideal_gas_kernel)
#define OPS_ACC3(x, y, z)                                                      \
  (n_x * 1 + x + (n_y * 1 + (y)) * xdim3_ideal_gas_kernel +                    \
   (n_z * 1 + (z)) * xdim3_ideal_gas_kernel * ydim3_ideal_gas_kernel)
//user function



void ideal_gas_kernel_c_wrapper(
  const double * restrict density,
  const double * restrict energy,
  double * restrict pressure,
  double * restrict soundspeed,
  int x_size, int y_size, int z_size) {
  #pragma omp parallel for
  for ( int n_z=0; n_z<z_size; n_z++ ){
    for ( int n_y=0; n_y<y_size; n_y++ ){
      for ( int n_x=0; n_x<x_size; n_x++ ){
        

  double sound_speed_squared, v, pressurebyenergy, pressurebyvolume;

  v = 1.0 / density[OPS_ACC0(0,0,0)];
  pressure[OPS_ACC2(0,0,0)] = (1.4 - 1.0) * density[OPS_ACC0(0,0,0)] * energy[OPS_ACC1(0,0,0)];

  pressurebyenergy = (1.4 - 1.0) * density[OPS_ACC0(0,0,0)];
  pressurebyvolume = -1.0*density[OPS_ACC0(0,0,0)] * pressure[OPS_ACC2(0,0,0)];
  sound_speed_squared = v*v*(pressure[OPS_ACC2(0,0,0)] * pressurebyenergy-pressurebyvolume);
  soundspeed[OPS_ACC3(0,0,0)] = sqrt(sound_speed_squared);

      }
    }
  }
}
#undef OPS_ACC0
#undef OPS_ACC1
#undef OPS_ACC2
#undef OPS_ACC3

