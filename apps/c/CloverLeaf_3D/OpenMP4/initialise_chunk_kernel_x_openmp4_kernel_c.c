//
// auto-generated by ops.py
//
#include "./OpenMP4/clover_leaf_common.h"

#define OPS_GPU

extern int xdim0_initialise_chunk_kernel_x;
extern int ydim0_initialise_chunk_kernel_x;
extern int xdim1_initialise_chunk_kernel_x;
extern int ydim1_initialise_chunk_kernel_x;
extern int xdim2_initialise_chunk_kernel_x;
extern int ydim2_initialise_chunk_kernel_x;

#undef OPS_ACC0
#undef OPS_ACC1
#undef OPS_ACC2

#define OPS_ACC0(x, y, z)                                                      \
  (x + xdim0_initialise_chunk_kernel_x * (y) +                                 \
   xdim0_initialise_chunk_kernel_x * ydim0_initialise_chunk_kernel_x * (z))
#define OPS_ACC1(x, y, z)                                                      \
  (x + xdim1_initialise_chunk_kernel_x * (y) +                                 \
   xdim1_initialise_chunk_kernel_x * ydim1_initialise_chunk_kernel_x * (z))
#define OPS_ACC2(x, y, z)                                                      \
  (x + xdim2_initialise_chunk_kernel_x * (y) +                                 \
   xdim2_initialise_chunk_kernel_x * ydim2_initialise_chunk_kernel_x * (z))

// user function

void initialise_chunk_kernel_x_c_wrapper(double *p_a0, int base0, int tot0,
                                         int *p_a1, int base1, int tot1,
                                         double *p_a2, int base2, int tot2,
                                         int x_size, int y_size, int z_size) {
#ifdef OPS_GPU

#pragma omp target teams distribute parallel for num_teams(OPS_threads)        \
    thread_limit(OPS_threads_for_block) collapse(3) schedule(static, 1)
#endif
  for (int n_z = 0; n_z < z_size; n_z++) {
    for (int n_y = 0; n_y < y_size; n_y++) {
      for (int n_x = 0; n_x < x_size; n_x++) {
        double *vertexx = p_a0 + base0 + n_x * 1 * 1 +
                          n_y * xdim0_initialise_chunk_kernel_x * 0 * 1 +
                          n_z * xdim0_initialise_chunk_kernel_x *
                              ydim0_initialise_chunk_kernel_x * 0;

        const int *xx = p_a1 + base1 + n_x * 1 * 1 +
                        n_y * xdim1_initialise_chunk_kernel_x * 0 * 1 +
                        n_z * xdim1_initialise_chunk_kernel_x *
                            ydim1_initialise_chunk_kernel_x * 0;

        double *vertexdx = p_a2 + base2 + n_x * 1 * 1 +
                           n_y * xdim2_initialise_chunk_kernel_x * 0 * 1 +
                           n_z * xdim2_initialise_chunk_kernel_x *
                               ydim2_initialise_chunk_kernel_x * 0;

        int x_min = field.x_min - 2;

        double min_x, d_x;
        d_x = (grid.xmax - grid.xmin) / (double)grid.x_cells;
        min_x = grid.xmin + d_x * field.left;

        vertexx[OPS_ACC0(0, 0, 0)] =
            min_x + d_x * (xx[OPS_ACC1(0, 0, 0)] - x_min);
        vertexdx[OPS_ACC2(0, 0, 0)] = (double)d_x;
      }
    }
  }
}
#undef OPS_ACC0
#undef OPS_ACC1
#undef OPS_ACC2
