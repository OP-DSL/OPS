//
// auto-generated by ops.py
//
#include "./OpenMP4/clover_leaf_common.h"
#include <omp.h>
#define OPS_GPU

extern int xdim0_update_halo_kernel2_xvel_plus_4_back;
extern int ydim0_update_halo_kernel2_xvel_plus_4_back;
extern int xdim1_update_halo_kernel2_xvel_plus_4_back;
extern int ydim1_update_halo_kernel2_xvel_plus_4_back;

#undef OPS_ACC0
#undef OPS_ACC1

#define OPS_ACC0(x, y, z)                                                      \
  (n_x * 1 * 1 + n_y * xdim0_update_halo_kernel2_xvel_plus_4_back * 1 * 1 +    \
   n_z * xdim0_update_halo_kernel2_xvel_plus_4_back *                          \
       ydim0_update_halo_kernel2_xvel_plus_4_back * 1 +                        \
   x + xdim0_update_halo_kernel2_xvel_plus_4_back * (y) +                      \
   xdim0_update_halo_kernel2_xvel_plus_4_back *                                \
       ydim0_update_halo_kernel2_xvel_plus_4_back * (z))
#define OPS_ACC1(x, y, z)                                                      \
  (n_x * 1 * 1 + n_y * xdim1_update_halo_kernel2_xvel_plus_4_back * 1 * 1 +    \
   n_z * xdim1_update_halo_kernel2_xvel_plus_4_back *                          \
       ydim1_update_halo_kernel2_xvel_plus_4_back * 1 +                        \
   x + xdim1_update_halo_kernel2_xvel_plus_4_back * (y) +                      \
   xdim1_update_halo_kernel2_xvel_plus_4_back *                                \
       ydim1_update_halo_kernel2_xvel_plus_4_back * (z))

// user function

void update_halo_kernel2_xvel_plus_4_back_c_wrapper(double *p_a0, double *p_a1,
                                                    int *p_a2, int tot2,
                                                    int x_size, int y_size,
                                                    int z_size) {
#ifdef OPS_GPU

#pragma omp target teams distribute parallel for
#endif
  for (int i = 0; i < y_size * x_size * z_size; i++) {
    const int id =
        omp_get_num_threads() * omp_get_team_num() + omp_get_thread_num();
    const int n_x = id % x_size;
    const int n_y = (id / x_size) % y_size;
    const int n_z = id / (x_size * y_size);
    double *xvel0 = p_a0;

    double *xvel1 = p_a1;

    const int *fields = p_a2;

    if (fields[FIELD_XVEL0] == 1)
      xvel0[OPS_ACC0(0, 0, 0)] = xvel0[OPS_ACC0(0, 0, 4)];
    if (fields[FIELD_XVEL1] == 1)
      xvel1[OPS_ACC1(0, 0, 0)] = xvel1[OPS_ACC1(0, 0, 4)];
  }
}
#undef OPS_ACC0
#undef OPS_ACC1
