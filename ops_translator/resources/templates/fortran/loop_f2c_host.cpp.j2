
{% block prologue %}
{% endblock %}

{% block kernel_wrapper %}
{% endblock %}

{% block host_prologue %}
//  ==================
//  Host stub function
//  ==================
#ifndef OPS_LAZY
extern "C"
void {{lh.kernel}}_host_c(
    const char *name,
    ops_arg *args,
    int nargs,
    int dim,
    int *range,
    ops_block block
)
{
#else
void {{lh.kernel}}_execute(ops_kernel_descriptor *desc)
{
{% block host_prologue_sub_1 required %}
{% endblock %}
    int dim = desc->dim;
    int *range = desc->range;
    ops_arg args[{{lh.args|length}}];
    {% for arg in lh.args %}
    args[{{loop.index0}}] = desc->args[{{arg.id}}];
    {% endfor %}
#endif

//  ======
//  Timing
//  ======
    double __t1, __t2, __c1, __c2;

    {% for arg in lh.args %}
    ops_arg arg{{arg.id}} = args[{{loop.index0}}];
    {% endfor %}

#if defined(CHECKPOINTING) && !defined(OPS_LAZY)
    if (!ops_checkpointing_before(args, {{lh.args|length}}, range, {{kernel_idx}})) return;
#endif

    if (block->instance->OPS_diags > 1)
    {
        ops_timing_realloc(block->instance, {{kernel_idx}}, "{{lh.kernel}}");
        block->instance->OPS_kernels[{{kernel_idx}}].count++;
        ops_timers_core(&__c1, &__t1);
    }

{% block host_prologue_sub_2 %}
{% endblock %}

//  =================================================
//  compute locally allocated range for the sub-block
//  =================================================
    int start_indx[{{lh.ndim}}];
    int end_indx[{{lh.ndim}}];
{% block host_prologue_sub_3 required %}
{% endblock %}

//  Range here is in C-style while start_indx and end_indx is Fortran style
#if defined(OPS_MPI) && !defined(OPS_LAZY)
    if ( getRange(block, start_indx, end_indx, range) < 0 ) return;
#else
    for (int n = 0; n < {{lh.ndim}}; n++) {
        start_indx[n] = range[2*n] + 1;
        end_indx[n]   = range[2*n+1];
    }
#endif

{% endblock%}

{% block host_loop required %}
{% endblock %}

{% block host_epilogue %}
{% endblock %}

{% block kernel_descriptor_lazy %}
#ifdef OPS_LAZY
extern "C"
void {{lh.kernel}}_host_c(
    const char *name,
    ops_arg *args,
    int nargs,
    int dim,
    int *range,
    ops_block block
    )
{

{% block kernel_descriptor_lazy_call required %}
{% endblock %}
}
#endif
{% endblock %}
