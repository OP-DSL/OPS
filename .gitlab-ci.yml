stages:
  - build
  - test

job_build:
  stage: build
  tags:
    - CCP, test
  script:
    - echo "Building the project"
    - source ~/.bash_aliases
    - module avail 

job_test:
  stage: test
  tags:
    - CCP, test
  script:
    - echo "Running tests"

